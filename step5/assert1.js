//这份代码继续了上一份代码里为完成的任务
//首先在这里列出API
//assert.equal (actual, expected[, message])
//第三个参数可以定义自己的提示信息

const assert = require('assert');

assert.equal(1, 2, '出错啦：1的值不等于2');

//执行以下，和上一份代码的结果对比一下。仔细观察，记忆。
